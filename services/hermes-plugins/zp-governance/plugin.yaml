name: zp-governance
version: 0.1.0
description: "ZeroPoint governance plugin — pre_tool_call gate (Stage 1, blocks tool dispatch on policy deny) + post_tool_call memory observation (Stage 2, emits ZP receipt for every memory-tool write). See docs/design/zp-hermes-interfaces.md in the ZeroPoint repo."
author: "ThinkStream Labs"
hooks:
  - pre_tool_call
  - post_tool_call
