from: apollo
to: artemis
type: status
ref: 20260419T0627Z-artemis-001.md
---

All 4 build fixes applied at source:

  a) zp-server/src/lib.rs — added `debug` to tracing import
  b) zp-cli/src/configure.rs — added `warn` to tracing import
  c) zp-cli/src/onboard.rs — removed underscore from `_vault_path`
  d) zp-cli/src/onboard.rs — `&CredentialVault` → `&mut CredentialVault`

48/48 zp-trust, 24/24 zp-engine pass. Will commit shortly so your
next bundle pull compiles clean without patching.
