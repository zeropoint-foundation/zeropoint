[package]
name = "default-gate"
version = "0.1.0"
edition = "2021"
description = "ZeroPoint v2 — default Tier 0 policy module (permissive)"

# This will compile to WASM in Phase 2.
# For now it's a regular Rust library used by zp-policy.

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
