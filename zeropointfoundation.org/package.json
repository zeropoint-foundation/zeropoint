{
  "name": "zeropoint-global",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute zpmail --local --file=migrations/0001_email_schema.sql",
    "db:migrate:remote": "wrangler d1 execute zpmail --remote --file=migrations/0001_email_schema.sql"
  },
  "dependencies": {
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.7.1",
    "postal-mime": "^2.4.1"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
